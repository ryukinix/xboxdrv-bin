## arg 1:  the new package version
post_install() {
    echo "[udev] loading new xboxdrv rules"
    udevadm control --reload-rules && udevadm trigger
}

## arg 1:  the new package version
## arg 2:  the old package version
post_upgrade() {
    post_install
}
